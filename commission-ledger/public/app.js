const $=s=>document.querySelector(s);const api=async(u,o={})=>{const t=localStorage.token;o.headers={...(o.headers||{}),...(t?{Authorization:'Bearer '+t}:{})};const r=await fetch(u,o);if(r.status===401){logout();throw Error('Session expired')}const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Request failed');return d};const money=n=>Number(n||0).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2});const today=()=>new Date().toISOString().slice(0,10);
function login(){document.body.innerHTML=`<div class="login"><form class="login-card" id="lf"><h1>Commission Ledger</h1><p class="muted">Fresh software edition</p><label>Email<input name="email" type="email" value="admin@local.test" required></label><br><label>Password<input name="password" type="password" value="Admin@12345" required></label><br><button class="primary" style="width:100%">Sign in</button><p id="le" class="muted"></p></form></div>`;$('#lf').onsubmit=async e=>{e.preventDefault();try{const d=await api('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});localStorage.token=d.token;localStorage.user=JSON.stringify(d.user);render()}catch(x){$('#le').textContent=x.message}}}
function logout(){localStorage.clear();login()} async function render(){document.body.innerHTML=`<div class="shell"><aside class="side"><div class="brand">Commission<br>Ledger</div><div class="nav"><button data-p="dashboard" class="active">Dashboard</button><button data-p="transactions">Transactions</button><button data-p="reports">Reports</button><button data-p="settings">Settings</button><button id="lo">Logout</button></div></aside><main class="main"><div id="view"></div><div class="footer">Standalone software • Database-backed • No spreadsheet formulas required</div></main></div>`;$('#lo').onclick=logout;document.querySelectorAll('[data-p]').forEach(b=>b.onclick=()=>page(b.dataset.p));page('dashboard')}
async function page(p){document.querySelectorAll('[data-p]').forEach(b=>b.classList.toggle('active',b.dataset.p===p));if(p==='dashboard')return dashboard();if(p==='transactions')return transactions();if(p==='reports')return reports();settingsPage()}
async function dashboard(){const m=await api('/api/metrics');$('#view').innerHTML=`<div class="top"><div><h1>Dashboard</h1><div class="muted">Live overview of your ledger</div></div><button class="primary" onclick="openTx()">+ New Transaction</button></div><div class="grid">${k('Balance',m.balance)}${k('Deposit',m.deposit)}${k('Withdrawal',m.withdrawal)}${k('Cash',m.cash)}${k('Upline',m.upline)}${k('Commission',m.commission)}${k('Deposit Count',m.depositCount,true)}${k('Withdrawal Count',m.withdrawalCount,true)}${k('Cash Count',m.cashCount,true)}${k('Upline Count',m.uplineCount,true)}</div><div class="card section"><h2>Commission logic</h2><p class="muted">Deposit commission = Deposit × (1% + 0.5% + 2%). Withdrawal commission = Withdrawal × 1.5%. Balance = Deposit − Withdrawal − Cash − Upline.</p></div>`}function k(a,v,int){return `<div class="card kpi"><div class="label">${a}</div><div class="value">${int?v:money(v)}</div></div>`}
async function transactions(){await loadTransactionsView()}async function loadTransactionsView(){const d=await api('/api/transactions');$('#view').innerHTML=`<div class="top"><div><h1>Transactions</h1><div class="muted">All ledger entries</div></div><button class="primary" onclick="openTx()">+ Add</button></div><div class="card"><div class="toolbar"><input id="search" placeholder="Search notes"><select id="type"><option value="">All types</option><option>deposit</option><option>withdrawal</option><option>cash</option><option>upline</option></select><input id="from" type="date"><input id="to" type="date"><button class="ghost" onclick="loadTable()">Filter</button><button class="ghost" onclick="location='/api/export'">Export CSV</button></div><div id="tbl"></div></div>`;window.loadTable=async()=>{const q=new URLSearchParams();['search','type','from','to'].forEach(x=>{const v=$('#'+x)?.value;if(v)q.set(x,v)});const r=await api('/api/transactions?'+q);$('#tbl').innerHTML=`<div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Type</th><th>Amount</th><th>Notes</th><th>Actions</th></tr></thead><tbody>${r.rows.map(x=>`<tr><td>${x.tx_date}</td><td><span class="badge">${x.type}</span></td><td>${money(x.amount)}</td><td>${escapeHtml(x.notes)}</td><td><div class="actions"><button class="ghost" onclick='openTx(${JSON.stringify(x)})'>Edit</button><button class="danger" onclick="delTx(${x.id})">Delete</button></div></td></tr>`).join('')||'<tr><td colspan="5" class="empty">No transactions</td></tr>'}</tbody></table></div>`};await loadTable()}
function escapeHtml(s){return String(s||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function openTx(x={}){$('#view').insertAdjacentHTML('beforeend',`<div class="modal open" id="modal"><form class="modal-card" id="txf"><div class="top"><h2>${x.id?'Edit':'New'} Transaction</h2><button type="button" class="ghost" onclick="closeModal()">×</button></div><div class="formgrid"><label>Date<input name="tx_date" type="date" value="${x.tx_date||today()}" required></label><label>Type<select name="type"><option ${x.type==='deposit'?'selected':''}>deposit</option><option ${x.type==='withdrawal'?'selected':''}>withdrawal</option><option ${x.type==='cash'?'selected':''}>cash</option><option ${x.type==='upline'?'selected':''}>upline</option></select></label><label>Amount<input name="amount" type="number" min="0.01" step="0.01" value="${x.amount||''}" required></label><label class="full">Notes<textarea name="notes" rows="3">${escapeHtml(x.notes||'')}</textarea></label></div><br><button class="primary">Save Transaction</button></form></div>`);$('#txf').onsubmit=async e=>{e.preventDefault();try{await api('/api/transactions'+(x.id?'/'+x.id:''),{method:x.id?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});closeModal();page('transactions')}catch(z){alert(z.message)}}}function closeModal(){$('#modal')?.remove()}async function delTx(id){if(!confirm('Delete this transaction?'))return;await api('/api/transactions/'+id,{method:'DELETE'});page('transactions')}
async function reports(){const m=await api('/api/metrics');$('#view').innerHTML=`<div class="top"><div><h1>Reports</h1><div class="muted">Period-based totals and commission</div></div><button class="ghost" onclick="window.print()">Print / Save PDF</button></div><div class="card"><div class="toolbar"><input id="rf" type="date"><input id="rt" type="date"><button class="primary" onclick="reportRun()">Generate</button></div><div id="report">${reportHtml(m)}</div></div>`;window.reportRun=async()=>{$('#report').innerHTML=reportHtml(await api('/api/metrics?from='+$('#rf').value+'&to='+$('#rt').value))}}function reportHtml(m){return `<div class="grid">${k('Balance',m.balance)}${k('Deposit',m.deposit)}${k('Withdrawal',m.withdrawal)}${k('Cash',m.cash)}${k('Upline',m.upline)}${k('Commission',m.commission)}</div>`}
async function settingsPage(){const s=await api('/api/settings');const pct=x=>(Number(x)*100).toFixed(2);$('#view').innerHTML=`<div class="top"><div><h1>Settings</h1><div class="muted">Commission rates used by the software</div></div></div><form class="card" id="sf"><div class="formgrid"><label>Deposit Rate 1 (%)<input name="deposit_commission_1" value="${pct(s.deposit_commission_1)}"></label><label>Deposit Rate 2 (%)<input name="deposit_commission_2" value="${pct(s.deposit_commission_2)}"></label><label>Deposit Rate 3 (%)<input name="deposit_commission_3" value="${pct(s.deposit_commission_3)}"></label><label>Withdrawal Rate (%)<input name="withdrawal_commission" value="${pct(s.withdrawal_commission)}"></label></div><br><button class="primary">Save Settings</button></form><div class="card section"><h3>Original spreadsheet rules carried into software</h3><p class="muted">The supplied workbook uses Deposit, Withdrawal, Cash, Upline and Final Balance. Default rates are 1%, 0.5%, 2% on Deposit and 1.5% on Withdrawal.</p></div>`;$('#sf').onsubmit=async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target));Object.keys(f).forEach(k=>f[k]=Number(f[k])/100);await api('/api/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});alert('Settings saved')};}
if(localStorage.token)render();else login();
